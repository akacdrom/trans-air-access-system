I put the docker file to the main directory because I had some problem about file locations in the directories

to create the docker container:
$ sudo docker build -t trans-air-docker .

to run the docker container
$ sudo docker run trans-air-docker
